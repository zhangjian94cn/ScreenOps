# ScreenOps 🚀

**ScreenOps** 是一个强大且智能的屏幕自动化工具，集成了 OCR 识别、图像匹配、操作录制以及基于规则的智能工作流。

## 🌟 核心特性

- **🖱️ 自动化点击** - 支持定点点击、坐标偏移、循环点击。
- **🔤 OCR 智能识别** - 使用 Tesseract OCR 识别屏幕文字并精准点击，支持模糊匹配。
- **📍 图像模板匹配** - 基于 OpenCV 识别屏幕上的图标或特定 UI 元素。
- **🚀 智能启动 (Smart Launch)** - 自动通过图标、文字或系统搜索唤起目标应用。
- **🎬 操作录制与回放** - 录制鼠标键盘操作并导出为 JSON5 脚本，采用百分比坐标适配不同分辨率。
- **🧠 智能工作流 (Automation)** - 通过 YAML 配置规则，实现“识别到特定状态则自动执行特定操作”的长链路自动化。

## 📦 安装指南

### 1. 克隆项目
```bash
git clone https://github.com/yourusername/ScreenOps.git
cd ScreenOps
```

### 2. 安装依赖
```bash
pip install -r requirements.txt
```

### 3. 系统依赖 (macOS)
- **Tesseract OCR**:
  ```bash
  brew install tesseract tesseract-lang
  ```
- **系统权限**: 
  请在“系统设置 -> 隐私与安全性 -> 辅助功能 & 屏幕录制”中授权给您的终端 (Terminal/iTerm2) 或 Python。

## 🚀 快速上手

### 1. 基础操作
```bash
# 获取当前鼠标坐标
python -m ScreenOps.main position

# 智能打开应用 (会自动尝试 OCR -> 图标匹配 -> Spotlight)
python -m ScreenOps.main smart-click "微信"
```

### 2. OCR 点击
```bash
# 点击包含“提交”文字的区域
python -m ScreenOps.main ocr-click "提交"
```

### 3. 录制与回放
```bash
# 开始录制 (Ctrl+C 结束)
python -m ScreenOps.main record -o scripts/demo.json5

# 播放脚本
python -m ScreenOps.main play scripts/demo.json5
```

### 4. 智能工作流
```bash
# 启动监控器，根据 rules.yaml 自动响应屏幕变化
python -m ScreenOps.main watch --rules workflows/examples/auto_login.yaml
```

## 📂 目录结构
- `core/`: 硬件控制与智能启动逻辑。
- `ocr/`: 文字识别模块。
- `image/`: 图像匹配模块。
- `automation/`: 规则引擎与上下文管理。
- `recorder/`: 录制与回放引擎。
- `resources/`: 存放图标样本等资源。
- `tests/`: 健康检查与逻辑测试脚本。

## 📄 开源协议
MIT License
