# ScreenOps AI 指令集 (AI Instructions)

如果你正在使用 AI Coding 工具（如 Claude, Cursor, GPT）来编写 ScreenOps 脚本，请将此文件内容提供给 AI，以获得满意的脚本生成效果。

---

## 🛠️ 基本 API 规范

```python
from ScreenOps import ops

# 1. 唤起应用
ops.open("应用名") 

# 2. 智能点击 (支持文字或图标文件名)
ops.click("目标文字") 

# 3. 链式操作示例
ops.open("微信").wait_for("通讯录").click("通讯录").type("搜索内容", enter=True)

# 4. 其它常用原子操作
ops.wait(2)                 # 等待 2 秒
ops.hotkey("command", "c")  # 快捷键
ops.screenshot("save.png")  # 截图
```

## 🧠 AI 编写建议 (Best Practices)

1.  **优先使用语义化点击 (`click`)**：除非是绘图等特殊场景，否则始终优先使用 `ops.click("文字")` 而不是坐标点击。这样生成的脚本具有极强的分辨率自适应能力。
2.  **利用链式调用**：ScreenOps 的接口支持 Fluent API，可以将一个完整的动作链写在一行内，代码清晰易懂。
3.  **增加 `wait_for` 缓冲**：在打开应用或点击可能导致界面跳转的操作后，建议使用 `wait_for("关键文字")` 确保界面已加载。
4.  **图标兜底**：如果某个按钮既没字又是自定义图标，请告知用户：“请截图该图标并将其放入 `resources/icons/` 目录下”，然后在代码中使用 `ops.click("文件名")`。

---

## 📋 典型 Prompt 案例

**场景：自动发送邮件**
> "帮我写一个基于 ScreenOps 的 Python 脚本。
> 流程：打开 '浏览器'，等待 '搜索' 框出现，
> 输入 'outlook.com' 并回车，
> 点击 '新邮件'，
> 输入收件人 'test@example.com'，
> 最后点击 '发送'。"

**AI 应当生成的代码：**
```python
from ScreenOps import ops

ops.open("浏览器") \
   .wait_for("搜索") \
   .type("outlook.com", enter=True) \
   .wait_for("新邮件") \
   .click("新邮件") \
   .type("test@example.com") \
   .click("发送")
```
